# Placeholder compose file for the scaffold stage.
#
# Real services should be added only after the first supported local workflow
# is defined. Until then, this file exists to reserve the deployment boundary
# without implying that Kafka, MongoDB, or any other infrastructure is already
# part of the supported developer experience.

services: {}
